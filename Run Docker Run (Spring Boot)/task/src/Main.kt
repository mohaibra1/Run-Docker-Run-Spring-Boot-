fun main() {
    // Provide the absolute path to the Dockerfile
    // Replace the string below with the actual path to your Dockerfile
    val dockerfilePath = "C:/Users/32492/IdeaProjects/Run Docker Run (Spring Boot)/Run Docker Run (Spring Boot)/task/src/Dockerfile"//"C:/Users/YourName/Desktop/hyper-web-app/Dockerfile"

    // for Windows use the following format
    // val dockerfilePath = "C:\\Users\\<USERNAME>\\path\\to\hyper-web-app\\Dockerfile"

    // Print the path to the Dockerfile
    println(dockerfilePath)
}

